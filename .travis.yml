language: go

go:
  - 1.4

install:
  # tools
  - make tools

  # project
  - make dependencies
  - make install

script:
  # linting
  - $(exit $(gofmt -l . | wc -l))
  - errcheck github.com/zimmski/go-mutesting/...
  - $(exit $(go tool vet -all=true -v=true . 2>&1 | grep --invert-match -P "(Checking file|\%p of wrong type|can't check non-constant format)" | wc -l))
  - $(exit $(golint ./... | grep --invert-match -P "(_string.go:)" | wc -l))

  # project
  - make test

  # code coverage
  - ginkgo -r -cover -skipPackage="testdata"
  - gover
  - if [ "$TRAVIS_SECURE_ENV_VARS" = "true" ]; then goveralls -coverprofile=gover.coverprofile -service=travis-ci -repotoken $COVERALLS_TOKEN; fi

env:
  # coveralls.io
  secure: "QjVY+pV3C84lbi9zsFcRllZ+8a9BcqUmhZw9LmGGczDdAiC/sRMiAk7E9uFuONzyQcZU9E2ayRkji+30tCT7F1+yPgE8uBxXL/JgOqKe8rG7UyUt2DchdQLNGE13hyp258cGIuLjnP7NwNXEisA2ocaZhPS/KzBTdoLS+Q3G3p0="
